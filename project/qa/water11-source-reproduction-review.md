# S48隔离重现：全部历史量测一致，文件并非全部同字节

2026-09-21。根唯一重算已完成，80.5437秒、CPU4、12帧、36文件/7,748,027字节、退出0，未触90秒。**24个文件同SHA，12个VDB不同SHA；正确状态是 REPRODUCTION_NOT_BYTE_RESTORATION。** 原路径仍为空，原缓存清除事故没有被改写。

- 12个config及12个mesh与事故前原文件逐项SHA一致，因此这些文件的解码字节也一致。12个VDB大小与原记录相同、SHA均不同。不能在缺少原VDB的情况下把差异推定为无关元数据。
- 已重新执行12帧完整只读审计，所有已归档逐帧字典与原S48审计**完全相等**，包含原始时基、源内湿面积/速度、四个截面的phi/MAC通量、mesh体积/截面/拓扑、FLIP数量与包络。原汇总也完全相等：Q中位0.46753327375013126m³/s，较原作者目标高56.1496%，CV=0.0006370217373122874。原±10%失败保留。
- 原VDB完整稠密数组哈希未在事故前保存；现在原字节缺失，无法证明新旧所有网格解码数组完全一致。此次额外归档了新VDB中可解码域网格的全数组哈希，供以后复查；`int32_trnc`点属性的Python绑定解码限制单列，不能伪称全部粒子属性已比对。第一次解码脚本遇到该限制的日志保留，修成逐网格记录不可读原因后完成，没有模拟或缓存写入。
- 新缓存审计前后SHA未变。684个其他缓存守卫通过；36个旧路径未回填，原场景、原审计和事故前哈希保留。当前获得的是可以重新检查的独立重现证据，不是原文件已恢复。

[逐文件/逐帧比较](water11-source-reproduction-review.json)、[新全12帧审计](water11-source-S48_reproduction01-audit.json)、[新解码字段哈希](water11-source-reproduction-decoded.json)。R075仍暂停，未追加模拟。完整水体几何可以使用已冻结岩床与作者C水形继续制作；它不依赖宣称源流量校准已经通过。

另已将**新重现36文件**复制到流体缓存目录以外的 [独立ZIP](water11-source-reproduction-cache-archive.zip)，每个成员解压字节逐SHA验证通过，[清单](water11-source-reproduction-cache-archive.json)可供未来恢复。该副本保全的是新重现数据，不能代称丢失的原S48 VDB；原缓存路径没有写回。
