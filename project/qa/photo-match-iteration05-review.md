# GEO-07 iteration05 独立复核

**GEO-07 仍为 NOT_RUN，五张焦点小样也未达到最终视觉验收。** 本轮实际打开 A10、A11、主客厅48、guest01/02/04图纸及五张05渲染；只读提取网格、一次有界相机诊断、一次原相机重投影。没有改建筑、原片、生产相机或主场景，没有渲染。

冻结场景：`scene/Fallingwater_iteration05.blend`，SHA-256 `6bcfee7841c22e8e2b636352cfca79ae94968afb4235fc20cdc57e250ae046ff`。Blender 5.2.1 LTS，4线程提取，哈希与整合者提供值完全一致。实际基网格见 `photo-match-mesh-iteration05.json`。

## Guest exterior A10：同一窗面低残差，独立屋檐仍不对应

放大原片可沿西到东数到连续8根竖框。新选择的是竖框与上下横框的实体交会，而不是此前混用的外压条和粗石轮廓；旧数据完整保留。所有新像素坐标在拟合前写入独立文件 `data/photo-match-points-iteration05.json`（SHA-256 `db487c3a2d8506601b15adf61e180dc07e6e49c7d269097088781089dbf6fa27`），随后先打开观测标点图。真实模型点使用实际网格边中点，验证两端确实属于同一条边，未凭空生成空间位置。

一次诊断固定用第0、2、4、7根框的上下端8点拟合，第1、3、5、6根框的上下端8点保留；另保留非共面屋檐候选 R01。相机仅优化旋转、眼位和焦距，主点固定原扫描中心，无裁切、透视拉伸或几何改动；眼位限制在外侧通道附近。所有17点都在相机前方。

| 分组 | 数量 | 残差/原片对角线 |
|---|---:|---:|
| 拟合窗端 | 8 | 中位0.4115% |
| 完全保留的窗端 | 8 | 中位0.4763% |
| 非共面屋檐 R01 | 1 | **18.386%，约232.50px** |
| 全部 | 17 | 中位0.4358% |

低总中位掩盖了一个严重保留点失败，不能报告“照片匹配通过”。8拟合点的世界秩为2，全部集中在同一南窗平面；7等分窗仍是C级构造，不是独立测得的每一历史窗宽。全部点凸包占原片17.58%，但单纯面积不能弥补共面问题。R01仍是U级屋檐纵向站位候选，因此该232.50px只能证明**当前对应/模型/镜头组合不一致**，不能作为直接拉伸屋檐的量值。

相机诊断眼位(1.5046,33.8981,7.8201)m，36mm长边等效24.77mm；它低于客房地坪8.4m，符合照片沿下方台阶向上看的可能方向，但真实站位还需要外侧梯段支持，不可直接作为生产相机。实际打开的叠图：`photo-match-guest_exterior_10-iteration05-guest10-diagnostic.png`；所有点与分组见 `photo-match-results-iteration05-guest10-diagnostic.json`。

具体待核：05南窗外面Y36.0266，低屋檐对应局部边Y35.7293，仅约0.2973m的外伸。A10可见屋檐覆盖较宽的外侧通道。应回到 **guest02 的低屋顶/露台外轮廓** 确认当前 `GUEST_LOW_ARM_ROOF` 是否用错了楼面边，以及R01确切纵向站位；不要为了匹配照片而猜宽度。原片两组外侧台阶(近处约236,895、远处约457–601,642)仍需要真实源对应对象，不能把泳池内的4级干台阶借作同一构件。

## Guest living A11：新增实体存在，仍不足8个可靠分散对应

已在05实际网格中确认真实 `GUEST_FIREPLACE_north_mass`、`corner_hood`、`hearth` 与西墙开口。原报告“未找到炉膛”这一存在性阻碍可以撤销；**存在性并不等于照片对应验收**。

- 原片炉口左上(571,350)、左下(571,412)有候选：分别为 `GUEST_L1_WEST_LOUNGE_lintel_0` 顶点2、`sill_0` 顶点6，世界位置约(3.8228,39.5673,9.48/8.60)。这里只记录候选，未标独立验证。
- 右炉口角被灯和器皿遮挡，继续排除，不计入拟合或8点门槛。
- 原片约(365,287)至(365,382)有可见窗转角；05西墙近南端是空门洞，不能任意把该窗角对应到南窗7等分中的某一根框。
- 三个天花/石墙交会(418,247)、(704,230)、(741,181)仍需确认图纸位置与真实面。低屋顶一块平板、炉膛高差以及北侧回墙不能互相替代。
- 炉岩随机突角、书架、椅子和遮挡推测点不计结构控制点。

因此A11本轮不进行相机拟合，不凑虚假的8点。标点图 `photo-match-guest_living_11-iteration05-observed.png` 保持橙色 unresolved；新候选和明确阻碍写入独立05点表。

## Main living48：保留原镜头，补查非共面独立石柱

从厨房朝南的图注和主层平面关系，画面左侧独立石柱最可能是 `MAIN_L1_south_stone_pier_1`，真实基网格范围X6.4452–7.2312、Y4.3266–4.8066、Z0.1–2.49m。石柱与灯屏不共面，适合后续独立控制；但照片下端被桌/椅和石皮遮挡，上端有石皮凸块及天花遮盖，当前不能确定哪个像素就是矩形基网格的哪一个角。

本轮**不重新拟合旧灯屏镜头**，将该柱全部8个真实角按旧镜头投影，保存 `photo-match-main_living_48-iteration05-pier-locked.png/json` 并实际打开。投影框范围约(232,192)至(302,360)，可见柱的顶/底交会仍明显偏上；橙色仅为观测区域，不是测量点，不计算假残差。该图继续揭示旧灯屏镜头不能代表完整空间。下一步必须由主楼作者确认石柱身份及可见面后，再决定可用的柱顶/柱脚控制点；未把任意石块轮廓提升为精确锚点。

## 五张真实焦点图的独立评分

全部为05场景的实际960×540、Cycles CPU、24样本、AgX小样。1=严重缺陷；2=明显模型感；3=中期可信；4=接近照片；5=最终照片级。评分只描述当前可见画面，不代替尺寸、隐藏几何、4K或移动镜头验收；没有平均后宣布通过。

| 实际PNG/曝光 | 建筑 | 材质 | 照明 | 环境/陈设 | 照片感 |
|---|---:|---:|---:|---:|---:|
| MAIN_B_BATH_B / EV1.6 | 3 | 2 | 3 | 2 | 2 |
| MAIN_L1_LIVING_B / EV2.4 | 2 | 2 | 3 | 3 | 2 |
| GUEST_L1_LOUNGE_A / EV2.4 | 2 | 2 | 2 | 2 | 2 |
| GUEST_POOL / 原PNG EV0.8 | 3 | 2 | 3 | 2 | 2 |
| MAIN_B_BATH_A / EV1.6 | 3 | 2 | 3 | 2 | 2 |

BathB同机位旧的两条宽黑地坪带已消失，这是可见的修复。洗脸盆、卫生间与门口地面可读；但器具轮廓和表面仍简化，两张图都裁掉部分器具，不能由此宣布全室完成。

主客厅炉口和石墙细节已可见，但大块表面仍过于均匀，规则石条近似机械切块。特别是炉前目前可见同心台阶式圆弧石台，与照片48的大块原生裸露岩石身份不相符；这是优先核查对象，不应靠裁镜避开。应结合main11及多角原片确定该圆弧台是否错误替代天然炉岩，再按源证据改造。

GuestLoungeA看向书柜与木屏，**新西北转角壁炉完全不在画面里**。这张图只能审核书架侧，不能关闭炉膛QA或充当A11对照机位。需东侧朝西、同时看见炉口、窗转角和地顶交会的实图。当前书本重复、家具简化，暗角也遮住了构造细节。

泳池外形与建筑关系可读，但水面主要是浅色反射块，当前小样看不出足够水深/边缘细节；近景叶片形状和树林分布仍有重复感。不从这一张静图推断动态水体或水下结构通过，也不把正常挑板阴影误报成几何孔洞。

每张图的绝对路径、SHA-256、实际打开记录、评分和P1建议见 `photo-match-iteration05-visual-review.json`。焦点图都属于中期验证，不能替代最终逐房图或照片级验收。
