# 主楼楼梯周边场地修正

已在独立第五轮场景副本上实际检查：15级 `MAIN_loggia_pool_stair_` 的84个身体/脚下采样，原26处失败，修正后0处失败；逐段横向身体射线也通过。只涉及该下行梯，**整条绕池路线与全61连接仍待第六轮复验**。

主依据为原始 main04 高分TIFF及实际查看的 `main04-loggia-bridge-source.jpg`。图纸显示向东下降的开敞梯段及其北侧挡墙；旧宽2.9m场地道路从桥斜穿梯段上空，属于C级道路数字化错误。修改 `data/site.json` 中北端桥路折线，使其从挡墙北侧接近Loggia；主桥结构、铺石、挡墙和原楼梯均未改变。新道路中心线和2.9m宽度仍是C级景观解释，不宣称测绘尺寸。

同时补充主泳池、干平台和梯段的地形排除范围：世界 X10.45–20.60、Y5.40–10.80，土面不高于Z−3.25。既有实际干平台Z−2.50，不能由土面升至入口标高来穿过平台。原建筑排除范围不足以覆盖东端低梯平台；旧道路又把土面抬高。此次独立检查副本仅改变220个相关地形顶点，最大向下差3.17277m，其余地形顶点保持不变。土面修正是与既有地下结构协调的C级解释，不是新增地形实测。

独立候选 `site-iteration06-candidate.blend` 只改两个对象：`SITE_Path_bridge_north_approach` 与 `SITE_Continuous_BearRun_Terrain`。源场景 SHA `6bcfee7841c22e8e2b636352cfca79ae94968afb4235fc20cdc57e250ae046ff` 保存不变。完整参数、逐采样负向/修正结果、220个顶点前后值见 `site-iteration06-check.json`；执行日志为 `site-iteration06-check.log`。原配置/脚本保留为 `site-iteration06-before.json/.py`。

完整重建还会重新安放植物并更新周围石块，因此不能用这个只改两对象的局部候选替代第六轮集成检查。主楼北干平台绕行靠墙过近、第二段楼梯首阶接入的旧路径问题单独交接路线作者，未删除池壁或首阶来让错误路径通过。
