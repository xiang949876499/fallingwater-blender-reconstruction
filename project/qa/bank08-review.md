# 08：真实桥北铺装边界与前坡局部修正候选

**状态：LOCAL_PATH_GEOMETRY_ACCEPTED_FOR_NEXT_CHECKPOINT / SURFACE_TINT_REJECTED / ENVIRONMENT_VISUAL_FAIL / NOT_INTEGRATED。** 根任务已经实际打开08的HERO、Loggia B、Living A三图，确认路径埋土消除与坡脚过渡是局部改善；灰色光滑坡面、植被和未修改的西坡仍未通过环境外观验收。本代理没有渲染或接入生产构建，旧灰面候选原样保留。

## 交付与来源

- 模型：`scene/Fallingwater_bank_candidate08.blend`，SHA256 **`b1d2085191e24132c76ce1765aad414b7298cd67ca3ea8ed392014bb4f490257`**。
- 完整来源：`scene/Fallingwater_iteration07.blend`，SHA256 `bf50530009e4a93315915815473188c91d891272ef2d8e9e0169b704bd2e2f16`，保留该版本West/Study、家具和天空；frame48进行同位检查。
- 下一整合调用：`near_terrain_detail.build_bridge_front08(ctx, {'enabled': True, 'surface_tint': False})`；`surface_tint`默认False，保留原地形材质。`BRIDGE08`整体仍默认关闭，没有修改build_scene.py/site.py/site.json或调用旧bank_detail/full30m覆层流程。当前helper SHA256 `fd56ffbb37741f5d80412d3e1a46c45ebaf9af66c3ad439e4093acb6fb63c77c`。保存的b1d灰面候选由旧helper `acc831577b8b5be188d2bf01a3bee8326270132931e3ebb738a9f73ef11f5775` 产生；其材质对应现在仅供历史复现的 `surface_tint: True`，原candidate-check脚本已显式写此开关。
- `ctx.root`实际指向 `qa/bank08-frozen-context`。07内嵌配置、site/main/guest JSON、site.py与fwlib.py已经一次性读出并逐项比对07的内嵌来源哈希；后续只读该冻结副本。没有读取或导入另一个代理正在修改的main_house.py，也没有把新地板改动混入本模型。完整记录见 `qa/bank08-frozen-inputs.json`。

## 改动对应的是实际可见前脸

07b失败的关键点已由精确像素射线定位：上一轮道路中心线保护再加网格guard，把所有五个可见点的Z和颜色都锁在旧状态。本候选按实际 `SITE_Path_bridge_north_approach` 边界，修正其外侧前脸与原本埋住路缘的高土；保持真实铺装顶面和中心线不动。

范围仅约 **x22.8…27.35、y8.5…13.7m**。为使真实铺装边界两侧能独立控制，只切分受交的局部三角：原108,900顶点/216,482三角变为108,945/216,572，即 **+45顶点、+90三角**；71个局部点下调，最大1.20000005m（浮点表示下约1.2m）。原范围以外且不触碰边界的 **216,304个三角世界几何哈希一致**，没有重建整个场地。

| 原诊断点 | 同一XY的原Z → 新Z（m） | 实际降低（m） | 新地表混合权重 |
|---|---|---:|---:|
| A，原像素357,136 | 2.564 → 1.467 | 1.098 | 1.000 |
| B，原像素400,200 | 1.586 → 0.703 | 0.883 | 0.989 |
| C，原像素400,280 | 0.448 → −0.019 | 0.467 | 0.144 |
| D，原像素310,220 | 1.308 → 0.878 | 0.431 | 1.000 |
| E，原像素460,240 | 0.949 → 0.277 | 0.672 | 0.895 |

C距实际铺装边仅83mm，因此颜色仍位于约0.30m路缘渐变带，不能把它当作未执行；它的高土本轮确已下降467mm。A/B/D/E主要前坡已经实际进入材质混合范围。上述是同世界XY读数，修改后同一图像像素可能命中坡后其他位置，必须实际渲染确认。

## 真实路缘横断与冻结检查

在A…E对应的实际路径边界法线上，取路内15/5cm、边界、外5/15/40/70/130/180cm共45个读数，原始数据在 `qa/bank08-cross-sections.json`。原路内5cm有100…372mm的埋土，本轮10个路内样本全部位于**原铺装下60mm**。新进程读回范围−60.005…−59.994mm；铺装实体的网格、Z、可见性不变。

外侧典型横断（相对铺装高度，A/B/C/D/E范围）：边界约−60mm，外5cm约−26…−23mm，外15cm约+41…+60mm，外40cm约+225…+268mm，外70cm约+561…+746mm，外130cm约+1.35…+1.64m。它把原本紧贴路缘陡起的高土转移为有明确坡脚的局部过渡；该坡形仍为C级作者近似，不能称为来源图实测剖面。

比较完整07的23,430个原对象：只有terrain、5组树干/配对树叶的贴地Z、两件聚合旧地被的局部顶点变化。没有新增对象；没有新增任何叶、树或岩体，也没有用新的巨岩或植被遮掩坡面。

- path/bridge、所有建筑和水对象、三个核心岩体、相机与其他原对象保持不变。
- 核心与两个肩岩新进程世界哈希仍为冻结07c的 `b85195ea…1337`、`9cf4fdb9…0270`、`05a2d60d…fedd`，完整值在 `qa/bank08-reopen.json`。
- 129个原相机的2m地面支撑邻域逐圈采样，实际terrain变化0m。
- 5组树根实际约位于新地面下12mm；15片旧落叶/105顶点重新贴地，其中5片做局部折角接触修正，面中心+1.255…+3.709mm。
- 旧苔藓70个三角/210顶点基部约+3mm；最高约54mm为保留的草丝尖端。没有全局压低或抬高地被。
- 07冻结路线59个主/补充段，对所有可能变化物体执行11,620条身体、头部、相机点/段射线，零新增障碍。其余物体已用哈希证明未改。这是变化回归，不替代根任务完整邻接及最终动画验收。

地表颜色直接通过原terrain上的连续属性控制，只有55个非零权重点，保持实际铺装/建筑内为0；没有额外底层sheet或ground面。现有草土地表扫描被局部去绿并混入棕灰有机色，仍不是经验证的落叶扫描或实测土壤。没有采用07b新增99,942片叶的全域方案。

## 下一步与边界

根任务已实际审阅`renders/previews/iteration08-bank`三机位小样：只采用路径几何及根部接触修复，不采用灰色材质；整体环境仍VISUAL_FAIL。本轮不重渲三图、不产生新的大场景。前述地表混合权重和颜色描述仅属于保留的b1d历史候选，不能当作获准的生产材质。

在根任务下一完整检查点上整合时，应使用该次构建自己冻结的配置/依赖，不用整个本候选覆盖之后完成的地板或水工作。只运行明确的08局部函数，不运行默认07b全域build。保存新版本并重验完整路线、实际路缘横断和根部；本次完整07来源的报告不自动适用于新来源。

具体顺序：site.build完成地形、路径、植被，asset_materials.apply完成原材质，再完成所有房间/总览相机及camera_review.integrate；**在camera_review之后、navigation.install之前**调用08函数。所有实际相机位置必须已写入且依赖图已更新，以便保留2m地面支持区。之后才验证、记录构建依赖、保存检查点和制作任何新的碰撞缓存。不要在site.build内部或相机创建之前调用；目前没有生产hook，由根任务加入。

`surface_tint: False`分支完全不调用ground_material，不新增权重属性、不复制材质；几何和既有植被接触步骤与b1d候选一致。成功后仅在terrain对象写入`fw_bridge08_applied`来源标记。相同场景再次启用会在任何修改前明确拒绝，避免分次累计降低；旧b1d候选即使没有新标记，也会被已有bank08权重属性拒绝。需要复跑时重开未修改来源或重新完整构建，不能仅删标记后再套一次；`enabled: False`一直是无操作。

验证入口`bank08-surface-toggle-check.py/json/log`只在内存打开完整07执行默认分支，再与保留b1d场景比较全部对象网格、逐面平滑标记、变换与渲染可见性；不保存新blend、不渲染。同时检查原地形材质槽/节点/连线与材质总数、无新增网格属性、重复调用和旧候选阻止修改。局部BMesh会清掉冗余的全平滑sharp_face属性，逐面平滑结果仍与b1d核对；两次初始属性列表断言失败日志保留，未影响任何模型文件。

该独立CPU4检查已PASS（23.16s）：23,430个对象与b1d网格/逐面平滑/矩阵/可见性差异为0，原材质节点和连线保持，新增网格属性为0；两个重复应用分支均在修改前拒绝，完整07与旧b1d文件哈希均不变。此结果验证已接受的结构修复可以独立应用，不将旧材质分支的图像结果视为新整合图像验收。

地形整体网格哈希已变化。若水缓存清单把整个terrain作为碰撞体，应由根任务按实际缓存清单核对，而不能仅凭三个核心岩体没变就跳过兼容性检查。源07、07b失败模型/图像、旧25叶负证据与本轮首个输入路径索引失败日志均保留。

证据入口：`bank08-frozen-inputs.json`、`bank08-candidate-check.json`、`bank08-cross-sections.json`、`bank08-reopen.json`；各有对应独立脚本/日志，全部位于qa目录。08无GPU/渲染，无生产钩子，无外部发布。
