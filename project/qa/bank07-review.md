# Bank07：实际贴地的近岸林下覆层候选

**当前状态：VISUAL_FAIL_NOT_ADOPTED。** 根任务实际检查 `renders/previews/iteration07-bank-cover` 的HERO、Living A、Loggia B三图后不采用：HERO出现硬边稀叶棕斑，Loggia B出现多边形贴片，原始圆丘与均匀草土依旧主导。作者也已实际打开同三图核对。模块继续默认关闭，未接入全场景；失败模型、图像和原始接触检查完整保留。

原几何检查 `PASS_LOCAL_CONTACT_AND_EXCLUSIONS_VISUAL_NOT_RUN` 是渲染之前的历史结果，只证明局部接触/排除；它没有被改写成视觉通过。下一步只读地形成因与单一候选提案见 `qa/bank07b-terrain-cause-and-proposal.md`。核心07c仍冻结为下一次水原型的阶段碰撞基线。

## 交付与独立开关

- 模型：`scene/Fallingwater_bank_candidate07.blend`，SHA256 `20027642d9aa54e3f48243889aedbe19c565acaa1ec71a511140364e69d25f38`。
- 基线：`scene/Fallingwater_geology_candidate07c.blend`，SHA256 `24a2ca01d276cde137ff3e041ecbf5bdc028289f5b83e8be20167cb4a0c561f3`。两者frame48、隐藏相同程序水；原相机/照明/曝光未改变。
- 新独立模块：`scripts/bank_detail.py`，SHA256 `c98214a2e67d1ba33b2ade16412cfddf98cf9e7fb9bbd2237c69a5eb140634d0`。默认 `enabled=False`，未在build_scene.py或site.py中添加自动调用。
- 本候选唯一启用参数与完整范围：`qa/bank07-candidate-settings.json`。实际运行/检查脚本与证据：`qa/bank07-candidate-check.py`、`.json`、`.log`。
- 原 `site.py` SHA256仍为 `2efde333707891a0a75237657b14057259ec2ae00e51c9df0ccc7db52858e1a6`；site.json仍仅启用阶段性的 `discontinuous_beds_v3`，SHA256 `2b4d5c712e7358dacc11458749d7dabc45bee98f43ec660fa9be4f36aa070c3d`。bank配置未写入该文件。

## 范围来自实际机位诊断

先从原07c对HERO、Living A、Loggia B各发出32×18机位射线，跳过透明/隐藏对象，记录真实可见地形点。记录在 `qa/bank07-visible-terrain-probe.json`；它是遮挡诊断而非渲染或逐像素可见性证明。

HERO主要近裸坡位于西侧约x−16…−8、y−4…3及南岸；Living A可以看到东南岸与桥侧，Loggia B可以看到桥北近坡。新覆层只在四个明确的局部多边形和主楼中心30m半径内生成，不向整个2km地形分布。

引用依据仍为已有 `research/interiors.md` §6 的落叶林/林下层证据、HABS西南87与东侧88照片。它们支持落叶、嵌入式角石、蕨类等地表类型；这里的具体密度、位置与腐殖土颜色为 **C级作者近似**，不当作实测现场植株或土壤反射率。

## 实际新增内容

总计 **43个对象**：

| 内容 | 实际数量 | 接触方式与限制 |
|---|---:|---|
| 薄有机底层 | 1对象、276面 | 复制选中原地形三角面并只抬升约2.5mm，保证面内部不跨过另一种三角划分；原地形与原材质不改。它复制已有CC0森林地表材质，只在新材质中弱化绿色、加入灰棕乘色，保持2m纹理尺度。 |
| 枯叶 | 1聚合对象、16,800片 | 12个局部簇，每簇1400片；叶长6.5–16cm，六三角面与微抬升中脉。逐顶点、三角中心、边中点检查真实BVH地形；跨过地形折角而产生浮空/深穿插的整片叶子被拒绝。 |
| 小角石 | 28对象 | 长约12–31cm的局部露出碎片，底部进入实际地形12mm；不重新调用随机大岩块/瀑布生成器。 |
| fern_02蕨类 | 13对象 | 4变体共享原mesh/material，无Decimate；陡坡和边界条件不满足的位置被拒绝，因此未强行凑满24株上限。 |

模型来源为已由独立代理完成官方MD5/本地SHA256与Blender离线读取检查的 Poly Haven CC0 `fern_02`。源文件 `assets/candidates/fern_02_2k/fern_02_2k.blend`，SHA256 `cf721e00ed5bb72f0b9c3fb59b8febc7b2b9b2082f162d5a80ba3a11e4e7028d`，对应 `qa/near-tree-asset07-offline-audit.json`。源模型的陈列XY已去除，4张贴图定位后打包进候选。

没有把完整mesh最低点误当植物根部：使用局部半径85mm、z<40mm的中心低位顶点作为**几何锚区代理**。这不表示已辨认生物学根/茎；该区域在实际地形上的局部间距允许约−5…+5cm，完整模型任意顶点不得深于地形5cm。保留源模型的少量负Z基部，避免为消除原本入土部分而把整株抬空。仍需要实际渲染检查叶片、透明裁切和根茎外观。

## 真实接触量测

检查在几何实际创建之后再次进行，没有用预设偏移量代替结果：

| 检查对象 | 样本 | 实际间距范围 |
|---|---:|---|
| 枯叶顶点与三角中心 | 218,400 | −0.898…+11.819mm，均值3.410mm |
| 底层顶点与三角中心 | 1,104 | +2.491…+2.507mm |
| 角石底顶点 | 196 | −12.0001…−12.0000mm |

落叶极少的亚毫米下陷来自地形三角边折角，低于本次1mm下陷限；最多11.8mm局部抬高由叶片中脉/折角造成，低于18mm局部上限。上述数值为相对实际地形的竖直间距，不是统一表面法向距离。旧 `_groundcover` 仅取 `_height+常数` 的方法没有被沿用。

全部 **131,825个新增世界顶点**再次通过四类排除：完整resolved建筑/客房/楼梯空间、桥与道路、河流带、冻结核心岩台包络；零失败。放置阶段还拒绝已有岩体高于地形的位置，避免覆层埋进既有露头。主要拒绝记录包括1550次过陡地形、189次叶片跨地形折角、38次已有岩体遮挡；未靠挪动原场景物体来接纳它们。

与源07c比较 **23,430个原对象**，网格/面索引/形态键、变换、材质绑定和render可见性变化为零。新增对象全部由 `fw_bank_detail` 所有；原地形、core/shoulders、建筑、交通和水网格均未改。

## 已完成的图像检查与未解决项

根任务已统一生成上述三个同参数frame48机位，本作者已实际打开：底层有生硬三角边，落叶范围形成贴片，整体裸坡未得到足够改善。三图和各自日志保留在 `renders/previews/iteration07-bank-cover`。接触PASS不等于图像PASS，此候选不采用。

当前候选仍没有解决所有近树冠形或大范围地形轮廓问题。建筑通行/头顶的全路线检查也没有在这次局部接触测试里重新执行；如果图像获准并集成，应在下一完整场景运行既有路径回归，而不能仅用“顶点在排除范围外”宣称漫游通过。
