# 16株近岸宽叶灌木独立候选08

**PHYSICAL_PASS / FRESH_READBACK_PASS / VISUAL_NOT_RUN / NOT_IN_PRODUCTION。** 整合者已批准制作这一个16株候选；尚未依据实际渲染图采用。地形材质仍为完整08的`FW_Continuous_Forest_Floor`，没有叠加已失败的落叶纹理候选。

候选：`scene/Fallingwater_shrub_candidate08.blend`，44,470,437 bytes，SHA256 `7dc113c93cab8dfae0ea73f1c797f92849d9e183f00a697040cfed346c06dc97`。

基线：`scene/Fallingwater_iteration08.blend`，SHA256 `c5cd501e4c8ae0205ecb2437cd2d06d0c92bcc9a87e7d93d3986aeef976070d7`，构建前后及新进程复开后均未改变。候选保留源保存帧**73**；同位预览必须显式使用**frame48**，不能直接按保存帧出图。

## 实际完成的局部形态

精确16株原根位见`terrain-material08-shrub-design.json`和`shrub08-candidate-check.json`，西岸8、桥岸8。只替换这16株的32个Branches/Leaves对象的data指向，所有matrix、根位、对象名、可见性和全场2400株understory数量保持原值。其余1184株普通灌木、1200株蕨类及全部成熟树不变。

| 新共享资产 | 采用实例 | 基生主干/末梢 | 最终叶数 | 枝干/叶/总三角 | 本地实际最高/最大冠半径（m） |
|---|---:|---:|---:|---:|---:|
| CANDIDATE08_RhododendronLike_2 | 10 | 5/20 | 236 | 1000/2832/3832 | 0.957/0.541 |
| CANDIDATE08_RhododendronLike_3 | 6 | 6/24 | 286 | 1200/3432/4632 | 1.057/0.534 |

两变体均低于5500三角/株。16株共4076片独立叶、66112三角；原16株为1456叶、12064三角。这里增加的是同16株内部的分枝和末梢叶片，不是新增林下植物或地面散叶。

每根基生主干有两级实际连接的侧枝，沿曲线共用截面；六边形父截面分为两个四边形子截面，四边形再分两个三边形末枝，分叉边界共用顶点/边。各基生主干都是封闭、连通、面绕序一致的木质网格系统，非流形边/开边/绕序不一致边均0。五或六根基生主干在同一入土区域起根，地下根团未建模，不能称为一个焊接根团。

每末梢为两段密集互生/螺旋叶群，不是严格同节点轮生。每叶11顶点/12三角，细窄叶柄与略弯的全缘叶身为连续网格；每片叶柄都接到最终木枝表面。设计叶身长13–17cm、宽3.5–5cm，另有12–17mm叶柄，不以扩大叶片掩盖旧直茎问题。保留旧枝材质与三种叶材质，只给新叶网格写对应UV，没有修改材质、图像、曝光或灯光。

根位、分枝、叶数与尺寸均为C作者候选。官方S12仅支持本地rhododendron存在；R. maximum是形态参考，精确物种/品种和各根位对应植物仍为U/C。源图及权威资料链接集中在`terrain-material08-shrub-design.md`，本任务未引入任何新下载资产。

## 实际坡面修正与物理结果

首次16株空间检查发现0558、1191、0055有少量叶片插入陡坡。没有降低terrain、抬根或放宽阈值：仅把新资产基部半径收窄到12.5–14.5mm，并对这两个新共享网格使用保守叶片集合。asset2删除设计叶154/160/170/182，asset3删除100/103，分别保留236/286叶。没有就地编辑原先各被600实例共享的旧网格；16株全部保留为新候选，不需要恢复失败株。

| 检验 | 结果 |
|---|---|
| 叶顶点及面中心对实际terrain BVH | 93,748点，最低离地18.095mm，阈值12mm |
| 叶面与地形三角相交 | 0 |
| 基部以上木枝对地形 | 最低净空21.069mm，非基部相交0 |
| 根锚与基部 | 根锚约−12mm，原XYZ/matrix精确不变；基部最深入土58.601mm，小于60mm限 |
| 叶柄到实际最终枝面 | 所有4076片，世界坐标最大距离0.002861mm |
| 道路及桥边界 | 来自保存路径网格；保守XY净空≥0.538m，实际顶点到铺装3D距离≥1.835m，根无铺装投影命中 |
| 相机保护 | 对所有保存相机，保守XY净空≥2.929m，阈值0.50m |
| 建筑/桥/核心/肩岩/旧水 | 实际网格包围盒先筛、可能相交者再测三角；无碰撞，main_plunge_and_stair排除体无XY重叠 |
| 原08导航 | 冻结08分段路线11,609条相机/身体/头部射线；被修改对象在基线和候选中命中均0 |

导航测试覆盖完整已存在的08分段路线与所有改动对象，其余对象指纹一致；它没有新增房间连通性或GUI自由行走验收。自然灌丛相互遮叠不代表可通行区域；此候选仍需原三图评估冠缘与局部遮挡。

## 非目标冻结与复开

CPU4构建对比23,384个对象，包含网格顶点/边/面/材质索引/UV及其他属性/shape keys、世界矩阵、对象属性、修饰器、约束、动画关联、材质、相机、灯光、世界、曝光、渲染设置和内嵌配置文本。只有批准的32个对象更换data；其计算所得dimensions和data hash随新网格改变，scale/matrix没有改变。旧共享网格、所有原材质和图像设置均未改。

另于frame48对63个核心/连续肩岩/旧水对象计算评估后的世界三角坐标hash，前后相同，新进程复开后再次相同。该hash使用按loop-triangle顺序编码的float32世界坐标，算法与早期其他报告的“mesh hash”可能不同；不能仅按不同算法的摘要字符串判断核心改动。

新CPU4进程从候选文件复开后，全量快照SHA256为`8351396b4337872700faad3fc48cd67973e74324ee1c3c2c29ee29ad29c7f99e`，与保存前一致；再次通过接触、地形、道路、相机和11,609条路线射线。没有渲染、修改生产脚本入口或保存复开后的场景。

## 给整合者的三图交接

对照`renders/previews/iteration08-focus`已有基线，不用重渲基线，也不用采用失败地面材质候选。按其benchmark使用frame48、960×540、32samples、AgX、原曝光及原相机，输出到一个新目录；CPU资源由整合者安排。

| 相机 | 曝光 | 判断重点 |
|---|---:|---|
| CAM_HERO | +0.8EV | 西岸8株以及桥岸是否由尖锥变为可读的不规则宽叶叶群；不能把仍裸露的岩土环境问题视作已修复 |
| CAM_MAIN_L1_LOGGIA_B | +0.8EV | 桥岸8株的末梢叶群、冠缘/分叉和路径遮挡，尤其0055对应近坡 |
| CAM_MAIN_L1_LIVING_A | +2.4EV | 近窗有3株可能经玻璃可见，防止新遮挡；不承诺修复约60m处灰色远景 |

当前**VISUAL_NOT_RUN**。模型保存成功、物理通过与新进程复开通过都不等于环境或植被最终验收。

## 重现与生产整合边界

`scripts/understory_detail08.py`只提供独立资产生成，不由生产`build_scene.py`导入。`qa/shrub08-candidate.py`从固定08和已冻结路线/排除数据构建；它拒绝覆盖已有候选。资产helper也拒绝同一场景重复创建同名资产。无需安装额外插件、下载贴图或启用GPU。

若三图获采用，后续整合应在site植被与最终terrain/root seating完成后应用、在导航验证/最终保存前检查。不可在新版本上只按序号盲替换：先验证16株的原data、根位和matrix与本名单相符，场地散布或建筑排除条件变动时重新验证；任何不匹配先停止整合，不能改其他人的建筑或挪动植物凑数。

本候选严格遵守“仅data变更”，因此旧对象自定义`plant_asset`标签仍是源资产标签；新资产的真实身份以`object.data.name`及新mesh的`origin_asset_index`为准。这个历史标签不是仍在使用旧几何的证据。后续如要更新标签，需纳入整合者明确的目标属性白名单。

## 文件与失败证据

- `shrub08-candidate-check.json`、`shrub08-readback.json`：当前有效物理/新进程检查。
- `shrub08-source-snapshot.json`、`shrub08-candidate-snapshot.json`：完整23,384对象详细快照；它们较大，作为QA证据保留，整合者可在发布包中按既有规则处理。
- `shrub08-prune-plan.json`：触坡叶片的实际逐实例证据及共享集合。
- `shrub08-frozen-route.json`：与完整08 SHA对应的原分段路线，未采用其他代理正在修改的路线。
- `shrub08-asset-preflight-attempt01.log`：最初自动skin枝干失败，出现断开部分、根部浮起且超预算；没有用于候选。attempt02为之后显式分叉的未修剪资产预检，最终数量以本交接为准。
- `shrub08-candidate-attempt01.json/.log`：探测器对不支持IDProperties的RNA调用items，未改模型即停止；已修正检测。
- `shrub08-candidate-attempt02.json/.log`：三株触坡，未保存场景；负证据完整保留。
- `shrub08-candidate-attempt03.json/.log`：物理通过，指纹检查把随data改变的计算dimensions当成额外手动修改；已只豁免该派生量，仍严格比较矩阵/scale及所有其他属性。
- `shrub08-candidate-attempt04.log`、`shrub08-readback.log`：最终成功构建/新进程复开。源场景两个内置图像的枚举警告及无渲染的Cycles CUEW探测警告保留；本任务无新增图像依赖或GPU操作。

原08、失败地面材质候选、所有原图与以上负证据均保留。本次收尾只维护本任务QA交接；AGENTS/STATUS/生产入口与其他代理负责文件不改。
