# Water12：完整水体制作尚未通过，已定位输入自交

2026-09-21。**尚未获得可宣称闭合通过的完整候选。** 未启动新液体缓存、未渲染、未修改生产或冻结岩岸。S48重现审计已经完成，见 [重现报告](water11-source-reproduction-review.md)；这里记录独立静态几何制作的实际失败，不能混成流体求解失败。

第一条制作链把原完整上下游水面冻结在frame48、加真实地形参考的厚度底体，并建立9个C作者重力支流。9支流的实际union都有正重叠体积，最后输入全河体233,725点、拓扑非流形边0、体积4,272.0061m³。**拓扑闭合并不足以证明该输入是有效的布尔实体。**

首次减去实际terrain顶面闭壳，输出反而增至44,819.1165m³，并有2,729条非流形边；terrain操作数自身体积40,447.3968m³、非流形边0。异常增加接近整块地形体积。后续岩岸减法没有消除该反例，于第57/114操作主动停止我自己的PID24740。原 [全链逐步记录](water12-complete-geometry01.json) 和 [日志](water12-complete-geometry01.log)保留，没有保存假称成功的全河候选。

随后只做一次近场修复尝试：近段输入14,322点，远段拟通过删除相接封口、共享62点截面接回，以免一公里程序河体参与CSG。启用exact self处理并加入每一步体积/非流形硬守卫。第一次setup遗漏操作数导致modifier disabled，在执行布尔前退出；该日志及setup报告保留，补上操作数后运行实际近场步骤。**第一支流union即产生355条非流形边，体积563.7572→563.3370m³，守卫立即保存反例并停止，没有再跑114步岩岸链。**

[失败场景](../scene/Fallingwater_water12_near_failed.blend) SHA256：`26d31f4108ad6d178ce95a20e0547ca7765b1e24bedb728c6f8505bf99b5daf5`，54,049,072字节。它保存失败时的输入与操作结果，旧程序水仍在，用于实体诊断，**不是完整水体渲染比较场景**。原计划 `Fallingwater_water12_complete_geometry01.blend` 没有被写成成功交付；最终source/bed/join收尾脚本尚未执行，不能误称那些检查已通过。

## 实际输入检查

在独立只读进程中，从失败场景保留的近场top网格重建相同体积，确认为原union之前的563.7572m³输入，然后测试实际渲染三角：

| 检查 | 近场上下游水体 | 新第1支流 |
|---|---:|---:|
| 非流形边 | 0 | 0 |
| 面积小于1e−12m²的退化三角 | 0 | 0 |
| 非相邻BVH候选对 | 834 | 0 |
| 经线段与三角实测确认的交叉对 | **829** | 0 |

近河体829个交叉对中，70个位于当前岩岸检查区、也位于旧water10的XY域内；15个交点高于z−4m。交点范围X[−48.7202,6.9137]、Y[−29.9117,−0.4492]、Z[−7.8745,−2.9491]。按初始上下皮索引分组：bottom/bottom653、bottom/side40、side/side2、side/top64、top/top70。上下游两个独立分量的有符号体积均为正，排除了“只有整块方向反了”的简单解释。

这证明继承条带河面并加底的输入存在实际自交，支持先重建输入，不能继续只修改Boolean阈值。**它没有独立证明每一个Boolean异常只由这一原因造成。** 检查跳过共享顶点的面及严格共面的重叠，因此829是已发现的交叉对，不是所有自交的完整清单；新支流的0结果也不升级为数学上无任何自交证明。详见 [实体输入审计](water12-solid-input-audit.json)，旧场景文件哈希前后相同。

## 下一项有限修复建议，未启动第三条制作链

保留一个上游供流、9个横向分区、共同冲击池与连续域外河段的范围。改变基础建模方法，不继续把旧曲线条带当可用CSG体：

1. 从实际原水面与真实岸床分开采样上游层和下游池层，先重建没有折返四边形的二维水位网格；以三角面表达同一XY位置的单层高度，原岩孔和实际上下游高差保留。不要用nearest-segment跳头或抬平台水面。
2. 近场初步格距5–7.5cm，真实源核心仍须按其体积预检≥3格；薄跌水支流继续用解析有厚度截面。规则水面网格的岸线须拟合真实岩岸，不能让阶梯格边直接成为最终可见河岸。
3. **在连接任何支流之前**先跑本次输入自交检查、闭合/体积分量/退化面检查；任一近场实交叉未解释就停。再对九支流使用明确共享环的实体接合，逐项检查，避免等全部114步后才发现输入无效。
4. 域外河段接环仍要共享顶点、删除内部重复封口；远处继承曲线的几何有效性单独披露。上游未来源体、贴床净空、平台干舷及下游完整轮廓先做实体量测，再交根安排实际图像。

该修复尚未制作或渲染，也没有改变现有water12大型原生预算提案的未批准状态。当前交付是可复查的失败场景、已确认的输入自交证据及下一具体方法；完整闭合候选、自然岸线接合、5–10秒动态连续性仍未完成。
