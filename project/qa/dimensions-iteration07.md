# 第七轮尺寸审计

已在冻结07（SHA256 `bf50530009e4a93315915815473188c91d891272ef2d8e9e0169b704bd2e2f16`）重新测量，并在第二个 Blender 进程重复验证。43条记录中22项数值匹配、21项仍留空；严格分类为14项通过、8项来源/端点精度受限。**GEO-02整体仍为INCOMPLETE**：悬挑、建筑开口、连接类别尚无有效锚点，场地只有泳池的部分覆盖。

本次只接受独立核读的 `MAIN_LEVEL_2`（9′4″）和 `GUEST_POOL_WIDTH`（13′0″）为“符合可读图纸标称目标”。最终实际测量对象分别是两露台完成面的相对标高和两条池内壳面；前者每次都重测两个finish顶部，不继承历史22mm饰面抵消的假设。来源、原字串、标称值和实际端点对象都要与接受记录一致，方可使用这一分类。阈值仍为 `max(0.020m,0.005L)`，没有调宽。

旧25/25.4mm记录原样保留在 `source_recorded_uncertainty_m`。数值抄读不确定度留NULL，字形状态为明确核读；绝对测绘精度UNKNOWN。未把未知填0，也未把符合图纸等同于现实建筑的绝对测量精度。其余八条房间标签的端点解释仍为C，没有随之升级。

中央CSV包含新增的原记录值、绝对测绘精度和标称复核来源列，旧CSV已另存。17项验证包括修改来源/字串/目标/端点后的拒绝、未知值保留、其他房间继续受限、缺类别继续未通过，以及冻结模型未改动。最终完成面实测见 `dimensions-iteration07-finish.json`、`dimensions-iteration07-finish-independent.json`、`dimensions-iteration07-finish-verification.json`，两进程均退出码0、17项PASS。较早结构板测法的07报告保留为历史。首次命令的模块查找失败保留在 `dimensions-iteration07.log`，已修正脚本目录导入。

地下外梯2′5″仍没有可对应的外侧弧形实体挡墙，不能使用踏步宽冒充墙间净宽。该缺口保留，后续按原图补建并独立重测。
