# 第10版尺寸读取适配

冻结场景1e7b17d9…中的客楼原整片 `GUEST_L1_MAIN_FLOOR` 和旧单跑tread01/11已由来源校正真正删除。首次旧量测器只得到10个数值，其余相关选择器NOT_RUN；`dimensions-iteration10.json/log` 保留。

量测器现在读取实际保留北/东两部分0基面，并检查两者真实顶面一致；不把南部降低平台或房间JSON中的z当作基准。旧场景仍读取旧完整板。第二次仅剩旧梯踏步定位失效，保留 `dimensions-iteration10b.json/log`。

已改为新两跑梯来源审查中72个实际完成面站位：原图Y377..400，三个高度由实测北部基面减.85/.55/.25米。每站重新对真实石/石皮和外挡墙投射，旧局部测量值不作为计算输入；72站只算一个2′5″锚点。当前10c得到23数值符合、15标准通过、8来源精度受限、21未测。总体GEO-02仍未完成。

原脚本备份 `dimension_audit-before10-selector.py`，所有三次报告保留。没有动模型或放宽任何宽度、名义标签及误差门槛；新露台/桥/斜bay测量将独立加入最终同场景清单。
